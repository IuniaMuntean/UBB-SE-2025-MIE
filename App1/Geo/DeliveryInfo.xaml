<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="App1.Geo.DeliveryInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1.Geo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Delivery Info">
    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!--<StackPanel Grid.Column="0" Spacing="10" VerticalAlignment="Top">
            <TextBlock Text="ID:" />
            <TextBlock Text="{x:Bind ViewModel.Id}" />

            <TextBlock Text="Manager:" />
            <TextBlock Text="{x:Bind ViewModel.Manager}" />

            <TextBlock Text="Departure:" />
            <TextBlock Text="{x:Bind ViewModel.Departure}" />

            <TextBlock Text="Destination:" />
            <TextBlock Text="{x:Bind ViewModel.Destination}" />

            <TextBlock Text="Distance:" />
            <TextBlock Text="{x:Bind ViewModel.Distance}" />

            <TextBlock Text="Driver:" />
            <TextBlock Text="{x:Bind ViewModel.Driver}" />

            <TextBlock Text="Departure Time:" />
            <TextBlock Text="{x:Bind ViewModel.DepartureTime}" />

            <TextBlock Text="Arrival Time:" />
            <TextBlock Text="{x:Bind ViewModel.ArrivalTime}" />

            <TextBlock Text="Truck ID:" />
            <TextBlock Text="{x:Bind ViewModel.TruckId}" />

            <TextBlock Text="Cargo Weight:" />
            <TextBlock Text="{x:Bind ViewModel.CargoWeight}" />

            <TextBlock Text="Cargo Status:" />
            <TextBlock Text="{x:Bind ViewModel.CargoStatus}" />
        </StackPanel>-->

        <StackPanel Grid.Column="1" Spacing="15" VerticalAlignment="Top">
            <Button Content="Generate Route" />
            <Button Content="Calculate Pricing" />
        </StackPanel>
    </Grid>
</Window>
