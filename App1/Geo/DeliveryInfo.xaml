<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="App1.Geo.DeliveryInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1.Geo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Delivery Info">
    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0">
            <StackPanel Spacing="10" VerticalAlignment="Top">
                <!-- Order Details -->
                <TextBlock Text="Order Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                
                <TextBlock Text="Order ID:" FontWeight="Bold"/>
                <TextBox Text="{Binding Order.OrderId, Mode=OneWay}" IsReadOnly="True"/>
                
                <TextBlock Text="Client Name:" FontWeight="Bold"/>
                <TextBox Text="{Binding Order.ClientName, Mode=OneWay}" IsReadOnly="True"/>
                
                <TextBlock Text="Cargo Type:" FontWeight="Bold"/>
                <TextBox Text="{Binding Order.CargoType, Mode=OneWay}" IsReadOnly="True"/>
                
                <TextBlock Text="Cargo Weight:" FontWeight="Bold"/>
                <TextBox Text="{Binding Order.CargoWeight, Mode=OneWay}" IsReadOnly="True"/>
                
                <TextBlock Text="Source City:" FontWeight="Bold"/>
                <TextBox Text="{Binding Order.SourceCity, Mode=OneWay}" IsReadOnly="True"/>
                
                <TextBlock Text="Destination City:" FontWeight="Bold"/>
                <TextBox Text="{Binding Order.DestinationCity, Mode=OneWay}" IsReadOnly="True"/>

                <!-- Delivery Details -->
                <TextBlock Text="Delivery Details" FontSize="20" FontWeight="Bold" Margin="0,20,0,10"/>
                
                <TextBlock Text="Driver:" FontWeight="Bold"/>
                <TextBox Text="{Binding Driver, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBlock Text="Truck ID:" FontWeight="Bold"/>
                <TextBox Text="{Binding TruckId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBlock Text="Departure Time:" FontWeight="Bold"/>
                <DatePicker Date="{Binding DepartureTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource DateTimeConverter}}"/>
                
                <TextBlock Text="Arrival Time:" FontWeight="Bold"/>
                <DatePicker Date="{Binding ArrivalTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource DateTimeConverter}}"/>
                
                <TextBlock Text="Distance (km):" FontWeight="Bold"/>
                <TextBox Text="{Binding Distance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource DecimalConverter}}"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Column="1" Spacing="15" VerticalAlignment="Top">
            <Button Content="Save Delivery Details" Click="SaveDeliveryDetails_Click"/>
            <Button Content="Generate Route" Click="GenerateRoute_Click"/>
            <Button Content="Calculate Pricing" Click="CalculatePricing_Click"/>
        </StackPanel>
    </Grid>
</Window>
