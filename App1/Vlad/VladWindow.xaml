<Window
    x:Class="App1.Vlad.VladWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1.Vlad"
    xmlns:model="using:App1.Vlad.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VladWindow">

    <Grid RowDefinitions="Auto, *" Padding="20">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Spacing="10">
            <TextBox x:Name="ClientIdBox" PlaceholderText="Id" Width="20"/>
            <TextBox x:Name="ClientNameBox" PlaceholderText="Client Name" Width="120"/>
            <TextBox x:Name="CargoTypeBox" PlaceholderText="Cargo Type" Width="120"/>
            <TextBox x:Name="CargoWeightBox" PlaceholderText="Weight (tons)" Width="120"/>
            <TextBox x:Name="SourceCityBox" PlaceholderText="Source City" Width="120"/>
            <TextBox x:Name="DestinationCityBox" PlaceholderText="Destination City" Width="120"/>
            <Button Content="Add" Click="AddOrder_Click"/>
            <Button Content="Add(Toma)" Click="AddOrder_Click_Toma"/>
        </StackPanel>

        <ListView x:Name="OrdersListView"
                  Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.Orders}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Order">
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="0,0,0,5" Padding="10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">

                            <TextBlock Text="{x:Bind OrderId}" Width="40"/>
                            <TextBox Text="{x:Bind ClientName, Mode=TwoWay}" Width="120"/>
                            <TextBox Text="{x:Bind CargoType, Mode=TwoWay}" Width="120"/>
                            <TextBox Text="{x:Bind CargoWeight, Mode=TwoWay}" Width="120"/>
                            <TextBox Text="{x:Bind SourceCity, Mode=TwoWay}" Width="120"/>
                            <TextBox Text="{x:Bind DestinationCity, Mode=TwoWay}" Width="120"/>

                            <Button Content="Update" Click="UpdateOrder_Click"/>
                            <Button Content="Delete" Click="DeleteOrder_Click"/>
                            <Button Content="Generate Route" Click="GenerateRoute_Click"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
